const EditorPage = ({ params }: { params: Promise<{ id: string }> }) => {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-gray-400">Canvas editor — coming in Phase 2</p>
    </div>
  );
};

export default EditorPage;
